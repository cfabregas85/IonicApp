import { FilterdonePipe } from './filterdone.pipe';

describe('FilterdonePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterdonePipe();
    expect(pipe).toBeTruthy();
  });
});
